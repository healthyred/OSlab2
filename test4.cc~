#include <iostream>
#include "thread.h"
#include <assert.h>

using namespace std;

int main()
{
  if (thread_libinit( (thread_startfunc_t) parent, (void *) 100)){
    cout << "thread_libinit failed\n";
    exit(1);
  }

}
